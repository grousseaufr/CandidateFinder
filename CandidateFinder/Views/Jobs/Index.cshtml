
@{
    ViewData["Title"] = "Job list";
}

@model List<CandidateFinder.BusinessLayer.Models.Job>

<h1>Current open jobs</h1>

<div class="job-list">
    @foreach (var job in Model)
    {
        <div>@job.Name</div>
    }
</div>